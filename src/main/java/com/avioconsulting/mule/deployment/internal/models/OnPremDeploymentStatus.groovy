package com.avioconsulting.mule.deployment.internal.models

enum OnPremDeploymentStatus {
    RECEIVED,
    STARTING,
    FAILED,
    STARTED,
    STOPPING,
    UNKNOWN,
    NOT_FOUND,
    NEVER_STARTED
}
