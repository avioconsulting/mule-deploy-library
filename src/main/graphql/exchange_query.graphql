query GetAssets ($asset_id: String!, $group_id: String!) {
	assets(asset: {
					groupId: $group_id,
                    assetId: $asset_id
                  }){
        assetId
        version
    }
}
